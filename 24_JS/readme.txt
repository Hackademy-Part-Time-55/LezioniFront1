LocalStorage


IntersectionObserver intersezione tra l'elemento target (il div vuoto) e l'elemento ancestor (di default la viewport)
L'IntersectionObserver è utile per implementare:
- infinite scroll
- lazy loading delle immagini